// API endpoint constants (paths relative to VITE_API_BASE_URL)
export const ENDPOINTS = {
  GET_PRODUCTS: '/products',
  SEARCH_PRODUCTS: '/products/search',
  GET_PRODUCT_BY_ID: (id) => `/products/${id}`,
}

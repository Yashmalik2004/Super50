#include<bits/stdc++.h>
using namespace std;

int func(int i, vector<int>&arr, vector<int>&curr, vector<vector<int>>&ans){
  int n=arr.size();
    if(i==n){
      ans.push_back(curr);
      return;
    }
}